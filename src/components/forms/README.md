# forms

مكونات form أولية بدون ربط بأي مكتبة خارجية.

- `FormField`: label/error/helper wrapper.
- `AppTextInput`: TextInput موحد يدعم label وerror وhelperText وdisabled وicons.

لا يوجد validation schema أو React Hook Form في Phase 2. أي ربط forms حقيقي يجب أن يتم في مراحل لاحقة حسب النطاق.
